{
  "name": "untitled",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

		"sass:expanded": "sass --watch sass/style.scss:css/style.css --style expanded",
		"sass:compressed": "sass --watch sass/style.scss:css/style.css --style compressed"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "sass": "^1.90.0"
  }
}
